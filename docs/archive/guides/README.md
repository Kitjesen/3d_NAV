# Guides

This folder is reserved for operator and developer how-to documents.

The previous Telegram and Feishu bot setup walkthroughs were removed when the underlying notification stack was retired (the dashboard now uses SSE plus optional WeChat Work, and Prometheus / alerting is on the roadmap — see `docs/08-project-management/TODO.md`).

When new operator-facing how-tos appear (e.g. dashboard diagnostic bundle, calibration field guide, NTRIP credential rotation), drop them here as English Markdown files.

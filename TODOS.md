# TODOS — LingTu Core Framework

Generated by /plan-eng-review on 2026-03-24.

## ~~TODO 1: Unify Transport abstractions (adapter pattern)~~ DONE

**Status:** Completed 2026-03-24
**File:** `src/core/transport/adapter.py` — TransportAdapter wraps any TransportABC backend to Transport Protocol. 7 tests in test_transport.py.

## ~~TODO 2: Rewrite DDS transport to raw cyclonedds~~ DONE

**Status:** Completed 2026-03-24
**File:** `src/core/transport/dds.py` — Rewritten to use cyclonedds-python directly. No rclpy dependency. Uses RawMessage IDL struct with pickle serialization. 6 tests in test_transport.py (skipped on Windows where cyclonedds not installed, run on S100P).

## ~~TODO 3: SHM buffer overflow should reject, not truncate~~ DONE

**Status:** Completed 2026-03-24
**File:** `src/core/transport/shm.py` — Now raises ValueError instead of silently truncating. Test updated in test_transport.py.

## ~~TODO 4: Backpressure — latest-value-only mode for In[T]~~ DONE

**Status:** Completed 2026-03-24
**File:** `src/core/stream.py` — In[T] now supports `set_policy("latest")` which drops messages when callback is busy. Tracks `drop_count`. 8 tests in test_core.py::TestBackpressure.

## ~~TODO 5: Remove sys.path hacks from test files~~ DONE

**Status:** Completed 2026-03-24 (partial — test files cleaned, module files kept)
**Files:** Removed `sys.path.insert` from 7 test files (test_registry.py, test_transport.py, test_blueprints_all.py, test_rings_modules.py, test_han_dog_module.py, test_navigation_blueprint.py, test_spec.py). conftest.py handles path setup centrally. Module files (han_dog_module.py, factory.py, etc.) keep their sys.path hacks for standalone usage outside colcon — remove those after verifying `pip install -e src/` on S100P.

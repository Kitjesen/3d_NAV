# Local model weights (optional)

Place ONNX / PyTorch / Horizon `.hbm` copies here for edge deployment. Large binaries are listed in `.gitignore` (`*.pt`, `*.onnx`, `*.hbm`, etc.).

Export flows live under `tools/export_yoloworld_bpu.py` and `tools/convert_onnx_to_hbm.sh`.

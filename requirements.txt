# LingTu 核心依赖 (S100P aarch64)
# 注意: numpy 限制 <2.0 以兼容 ele_planner.so (要求 numpy 1.x)
numpy>=1.24,<2.0
opencv-python-headless>=4.5
PyYAML>=6.0
scipy>=1.10
scikit-learn>=1.0

# LLM
openai>=1.0
requests>=2.28

# 语义导航
jieba>=0.42

# 可选: Agent 模式 (pip install -r requirements-agent.txt)
# langchain-core>=0.2
# langchain-openai>=0.1
# pydantic>=2.0

name: Python Tests + Lint

on:
  push:
    branches: [main]
    paths: ['src/**', 'cli/**', 'docs/**', 'calibration/**', 'pyproject.toml', '.github/workflows/test-python.yml']
  pull_request:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint:
    name: Ruff Lint + Format
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          submodules: false
          sparse-checkout: |
            src
            cli
            docs
            calibration
            pyproject.toml
          sparse-checkout-cone-mode: false
      - uses: astral-sh/ruff-action@v3
        with:
          args: check src/ cli/ docs/ calibration/

  test:
    name: Framework Tests
    runs-on: ubuntu-22.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          submodules: false
          sparse-checkout: |
            src
            cli
            config
            sim
            pyproject.toml
          sparse-checkout-cone-mode: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: pip
      - name: Install dependencies
        # opencv-python-headless: required by bpu_detector.py (module-level
        #   import), reachable via mock.patch in test_perception_*.py.
        # pydantic: required by gateway_module.py (BaseModel/Field/
        #   field_validator), reachable via test_gateway_helpers.py +
        #   test_cross_module_integration.py.
        run: pip install numpy scipy pyyaml pytest opencv-python-headless pydantic
      - name: Run core tests
        run: python -m pytest src/core/tests/ -q --tb=short
        env:
          PYTHONPATH: src
